# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
# CLI binary built at the repo root (`go build -o github-analytics ./cmd/github-analytics`).
# Anchored with a leading slash so it does NOT also ignore the cmd/github-analytics/ source dir.
/github-analytics
# Server binary built from ./cmd/server (e.g. `go build ./cmd/server`).
/server

# Test binary
*.test

# Output directory
*.out

# Coverage files
coverage.out
coverage.html

# Go workspace file
go.work

# Node.js
node_modules/
# pnpm-lock.yaml はバージョン管理に含める（依存関係の再現性のため）
yarn.lock
package-lock.json

# Environment variables
.env
.env.local
.envrc

# Output files
output/
*.csv
*.txt

# Skills directories (tracked, but ignore if needed in future)
# .claude/skills/
# .cursor/skills/
